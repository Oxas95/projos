if(getpid()==banque_pid)
{
while (a<nb_main)
{

	if (nb_carte<30)
	{

		deck =init_deck
		}


	for(int i...) distribuer carte en vidant main précédante avec banque aussi
//ici tous les processus sont déjà arrêtés donc on fait

	for(i=0;i<nb_joueur;i++)
	{
		while(joueur[i].action!=rester)
		{	

			kill(joueur[i],SIGCONT);
			read(pipe,joueur[i].action);
			if piocher write(pipe,cartepioché)
			kill(joueur[i],SIGSTOP)		
		}	
			


	}

	

}


if pid ==joueur[0]
if pid ==joueur[1]
if pid ==joueur[2]
if pid ==joueur[3]

//pour les trois c'est les mêmes mais ça permet d'être sûr
while(jouer.action!=rester)
{
kill(banque,SIGSTOP)
write(pipe,jouer)// la fonction jouer va  renvoyer si on joue ou pas
kill(banque,SIGCONT);
read(pipedelecture,la carte que j'ai pioché)
}



}